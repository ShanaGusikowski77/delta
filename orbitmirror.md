# GoLinker 资源导航系统

GoLinker 是一个面向技术社区与研发团队的开源外链资源导航与聚合管理平台，专注于解决多源技术文档、社区教程、视频资料与官方站点分散、难以统一检索和版本追踪的问题。项目定位为轻量级、可私有化部署的资源目录引擎，适用于中小型开发团队、开源项目文档站以及个人知识库管理员。

目标用户包括技术文档维护者、DevOps 工程师、开源社区贡献者以及希望建立自维护资源索引的开发者。通过结构化的资源录入与分类机制，GoLinker 将散落于各域名的优质外部链接转化为可检索、可注释、可共享的内部知识资产，显著降低信息查找与传递成本。

## 功能概览

- **多源资源收录**：支持以域名或完整 URL 为最小粒度录入外部资源，自动识别协议与子域名，保留原始访问路径。
- **分类与标签系统**：提供可自定义的层级分类（如视频、文档、社区、工具）和扁平标签体系，支持一对多关联。
- **资源状态监控**：定期探测已收录 URL 的可访问性，在管理面板中标记异常链接，辅助维护资源有效性。
- **快速检索与过滤**：基于标题、分类、标签、录入时间的多维度搜索，支持模糊匹配与精确查询。
- **导入与导出机制**：支持批量导入 URL 列表（纯文本或 CSV），导出为 Markdown 表格或 JSON 格式用于备份或迁移。
- **访问统计与热度排序**：记录资源被点击次数，按周/月聚合，帮助识别高频使用资源。
- **只读前端展示模式**：提供面向最终用户的只读页面，隐藏管理入口，适合嵌入团队文档站或仪表盘。
- **RESTful API 接口**：所有资源数据可通过 JSON API 获取，便于与其他系统集成或二次开发。

## 应用场景

1. **团队文档站外链整合**：技术团队在内部 Wiki 中维护了大量外部参考链接（如 API 文档、性能测试报告、镜像站地址），GoLinker 可作为独立导航层统一管理，避免 Wiki 页面频繁修改。
2. **开源项目资源附录生成**：开源项目维护者可将项目依赖的第三方库、学习资料、视频教程录入系统，自动生成结构化的资源列表章节，直接嵌入 README 或项目官网。
3. **个人知识库外部引用索引**：研究员或高级开发者利用 GoLinker 管理阅读过的技术博客、论文链接、会议录像，按主题分类并附加个人备注，构建可检索的个人外链库。
4. **社区活动资料归档**：技术社区运营者将历次分享活动的幻灯片链接、回放视频、代码仓库地址集中收录，生成按活动日期或主题排序的公开资源页。
5. **离线环境资源缓存规划**：在受限网络环境下，运维人员可通过 GoLinker 导出所有外链列表，配合下载工具规划离线缓存任务，确保关键资料本地可用。

## 快速开始

以下步骤适用于 Linux / macOS / Windows WSL 环境，需提前安装 Git 与 Node.js（v16+）。

```bash
# 克隆项目仓库
git clone https://github.com/golinker-io/golinker.git
cd golinker

# 安装项目依赖（使用 npm）
npm install

# 复制环境变量模板并修改数据库连接
cp .env.example .env

# 初始化数据库表结构并导入示例资源
npm run db:init

# 启动开发服务器（默认监听 3000 端口）
npm run dev
```

访问 `http://localhost:3000` 即可进入前端展示页，管理后台入口为 `/admin`，默认管理员账号 `admin@localhost`，密码 `changeme`（首次登录强制修改）。

## 安装要求

| 依赖组件 | 必需版本 | 说明 |
|---------|---------|------|
| Node.js | v16.20.x 或 v18.x LTS | 运行时环境，建议使用 LTS 版本 |
| npm | v8.x 或 v9.x | 包管理器，随 Node 自动安装 |
| PostgreSQL | v13.x 及以上 | 主数据库，用于存储资源、分类与统计 |
| Redis | v6.x 及以上 | 缓存与会话存储（可选，生产环境推荐） |
| Nginx | v1.18+ | 反向代理与静态资源服务（生产部署建议） |
| Git | v2.25+ | 版本控制，用于克隆与更新代码 |

## 文档导航

| 层面 | 目录 | 回答的问题 |
|------|------|-----------|
| 用户手册 | `/docs/user-guide/` | 如何录入资源、分类管理、检索使用、导出数据 |
| 管理员指南 | `/docs/admin-guide/` | 用户权限配置、链接监控告警、系统备份与恢复 |
| 开发者文档 | `/docs/developer/` | API 接口规范、插件扩展机制、自定义主题开发 |
| 部署运维 | `/docs/deployment/` | 容器化部署（Docker Compose）、系统调优、日志管理 |

## 资源列表

本系统预置了一批示例资源链接，收录于初始化数据中。这些链接属于公共互联网上的技术视频与影视资料类站点，仅作为资源格式演示用途。用户可按需替换或删除。

视频类资源

<code>yijiabifenzhibo.org.cn</code>

<code>fajiabifenzhibo.org.cn</code>

<code>guochanjingpinzaixianmianfeikan.org.cn</code>

<code>zhongwenzimuzaixianyingshiyuan.org.cn</code>

<code>mianfeiguankanzaixianguankan.org.cn</code>

<code>jiujiushipinzaixianguankan.org.cn</code>

<code>oumeizaixianguankanshipin.org.cn</code>

## 项目结构

```
golinker/
├── src/
│   ├── core/                    # 核心模块：配置、日志、数据库连接池
│   ├── routes/                  # 路由层：前端页面路由与 API 路由分离
│   │   ├── web/                 # 只读前端页面路由（资源列表、详情、搜索）
│   │   └── api/                 # RESTful API 路由（资源增删改查、分类管理）
│   ├── controllers/             # 控制器层：处理请求参数校验与响应组装
│   ├── services/                # 业务逻辑层：资源索引、状态监控、统计聚合
│   ├── models/                  # 数据模型层：基于 Sequelize 的实体定义（资源、分类、标签）
│   ├── middlewares/             # 中间件：身份验证、日志记录、跨域处理
│   ├── utils/                   # 工具函数：URL 规范化、定时任务调度、导出生成器
│   └── views/                   # 前端模板引擎文件（EJS）
├── config/                      # 环境配置文件（开发、测试、生产）
├── migrations/                  # 数据库迁移脚本（版本化 DDL）
├── seeders/                     # 初始数据种子文件（含示例资源列表）
├── tests/                       # 单元测试与集成测试（Jest + Supertest）
├── docker/                      # Dockerfile 与 Compose 编排文件
├── docs/                        # 完整文档（用户手册、管理员指南、开发者文档）
├── .env.example                 # 环境变量模板
├── package.json                 # npm 依赖与脚本定义
├── README.md                    # 项目入口文档（本文件）
└── LICENSE                      # MIT 许可证文本
```

## 贡献指南

1. **提交 Issue 讨论**：在 GitHub Issues 中描述您发现的问题或希望新增的功能，等待维护者确认方向后再进行代码改动，避免无效 PR。
2. **派生仓库并创建特性分支**：从主仓库的 `main` 分支派生（Fork）到个人账户，然后创建以 `feat/` 或 `fix/` 为前缀的分支名，例如 `feat/export-csv`。
3. **编写测试与更新文档**：对于新增功能或修复，需在 `tests/` 目录下补充对应测试用例，并同步更新 `docs/` 中的相关文档章节。
4. **提交 Pull Request**：确保所有 CI 检查通过（包括 ESLint、单元测试、构建测试），PR 描述中明确关联对应的 Issue 编号。
5. **签署开发者原创声明**：首次贡献者需在 PR 评论中确认代码为原创且遵循 MIT 许可，维护者会在一周内完成代码审查与合并。

## 常见问题

**Q：是否必须使用 PostgreSQL？能否替换为 SQLite 或 MySQL？**

当前版本仅对 PostgreSQL 做了充分适配，包括 JSONB 字段索引与特定聚合函数。若希望使用 SQLite 或 MySQL，需要自行修改 `models/` 中的数据类型定义和部分查询语句，官方暂不提供完全兼容性保证。对于小型测试环境，可使用 Docker 快速拉起 PostgreSQL 容器。

**Q：资源状态监控会影响页面访问速度吗？**

监控任务通过独立的定时工作线程执行（默认每 6 小时一次），不会阻塞主请求线程。在管理面板中查看监控结果时会实时查询最新缓存状态，若需手动触发探测，可在后台点击"立即检测"按钮，该操作会异步执行并返回任务 ID。

**Q：如何将现有的大量 URL 列表快速导入系统？**

系统支持通过命令行脚本批量导入纯文本文件（每行一个 URL）或 CSV 文件（含标题、分类、标签列）。具体命令为 `npm run import -- --file ./links.txt --type text`，详细参数请参考 `/docs/admin-guide/batch-import.md`。导入前建议先用 `--dry-run` 模式预览解析结果。

## 许可证

MIT

> 外链数量: 7 | 生成时间: 2026-08-14 22:01:34
