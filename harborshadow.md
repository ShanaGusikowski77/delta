# OpenLink Navigator

OpenLink Navigator 是一个面向技术调研、数据聚合与信息验证场景的轻量级外链资源汇总与健康度监控平台。项目定位为开发者、运维人员及技术决策者提供一组高可用的结构化外部数据入口，帮助用户快速定位特定垂直领域的实时状态页面、数据接口与备选源。

项目不提供数据存储或二次加工能力，聚焦于对原始 URL 资源的组织、分类、可用性探测与访问策略建议。目标用户包括需要依赖外部比分、状态页或公开数据源进行集成测试、演示环境搭建或监控看板构建的工程团队。

## 功能概览

- **多源资源聚合**：支持将分散在多个域名下的同类数据页面聚合为统一的资源清单，便于批量管理与切换。
- **可用性探测**：内置 HTTP 状态码与响应时间检测逻辑，辅助识别资源当前的可访问状态。
- **分类标签系统**：允许为每个资源附加业务类别、地域或备用层级标签，支持自定义分组。
- **原始地址透传**：所有外部链接以原始字符串形式展示，不做自动补全或改写，确保调试与对接时地址语义一致。
- **快速克隆部署**：提供完整的项目模板与启动脚本，可在本地或内网环境中 5 分钟内完成实例搭建。
- **资源变更日志**：记录每次资源列表的增删改操作，便于审计与回溯。
- **纯静态输出模式**：支持生成静态 HTML 目录页，可直接挂载至 Nginx 或对象存储服务，降低维护成本。

## 应用场景

- **内部运维仪表盘补充**：运维团队可将本项目作为统一入口，在内部监控系统中嵌入资源列表，用于快速跳转至外部状态页面，避免在多个浏览器标签间反复切换。
- **自动化测试的环境依赖管理**：集成测试套件可调用本项目提供的资源清单，在测试启动前对依赖的外部地址进行连通性预检，若不可达则自动切换至备用地址。
- **技术调研的信息归集**：架构师在进行竞品分析或技术选型时，可使用本项目整理候选服务商的公开数据页面，形成结构化的调研文档，便于团队评审。
- **演示环境的快速搭建**：售前或解决方案工程师可将本项目作为演示环境的一部分，向客户展示如何从多个公开数据源获取实时信息，无需自建后端数据服务。

## 快速开始

以下步骤适用于 Linux/macOS 环境，若使用 Windows 建议通过 WSL2 或 Git Bash 执行。

```bash
# 克隆项目仓库
git clone https://github.com/openlink-navigator/navigator-core.git
cd navigator-core

# 安装依赖（基于 Node.js 18+）
npm install

# 启动开发服务，默认监听 3000 端口
npm run dev
```

启动后访问 `http://localhost:3000` 即可查看资源列表首页。生产环境部署可使用 `npm run build` 生成静态文件，输出目录为 `dist/`。

## 安装要求

| 依赖项 | 必需版本 | 说明 |
|--------|----------|------|
| Node.js | 18.x 或 20.x LTS | 运行时环境，用于执行构建脚本与开发服务器 |
| npm | 9.x 或 10.x | 包管理器，用于安装项目依赖 |
| Git | 2.25+ | 用于克隆仓库及版本管理 |
| 操作系统 | Linux / macOS / Windows WSL2 | 推荐使用 Unix-like 环境以获得最佳兼容性 |
| 浏览器 | Chrome 90+ / Firefox 88+ | 仅用于预览静态页面，无后端依赖 |
| 网络 | 可访问外网 | 用于检测资源列表中各 URL 的连通性 |
| 磁盘空间 | 50 MB 以上 | 包含源代码、依赖缓存及构建产物 |

## 文档导航

| 层面 | 目录 | 回答的问题 |
|------|------|------------|
| 用户手册 | `/docs/guide/` | 如何添加/删除资源、如何进行批量导入、如何配置探测间隔 |
| 部署指南 | `/docs/deployment/` | 支持哪些部署方式（Docker、Nginx、OSS）、如何配置反向代理 |
| API 参考 | `/docs/api/` | 资源列表的数据结构定义、探测结果 JSON 格式、回调接口约定 |
| 运维说明 | `/docs/ops/` | 日志如何查看、健康检查如何配置、异常告警如何接入 |
| 设计文档 | `/docs/design/` | 为什么采用纯静态架构、资源分类的设计考量、地址处理策略 |

## 资源列表

以下为项目当前收录的全部原始数据地址，按业务类别分组展示。所有地址均严格保持用户提供的原始格式，未做任何改写或补全。

### 篮球相关数据页面（主域）

- <code>lanqiubifend.org.cn</code>
- <code>lanqiubifene.org.cn</code>
- <code>lanqiubifenf.org.cn</code>
- <code>lanqiubifeng.org.cn</code>
- <code>lanqiubifenh.org.cn</code>

### 足球相关数据页面（主域）

- <code>zuqiubifenziboa.org.cn</code>
- <code>zuqiubifenzibob.org.cn</code>

## 项目结构

```
navigator-core/
├── src/
│   ├── core/                 # 核心逻辑：资源加载、解析、去重
│   ├── probe/                # 可用性探测模块：HTTP 请求、超时控制
│   ├── classifier/           # 分类器：基于域名或关键字自动打标签
│   ├── render/               # 渲染引擎：生成静态 HTML 或 JSON 目录
│   └── utils/                # 工具函数：日志、配置、地址规范化（仅内部）
├── config/
│   ├── resources.json        # 主资源清单，编辑此文件以增删地址
│   └── probe.config.js       # 探测参数：超时时间、重试次数、并发数
├── docs/                     # 完整文档，见上文文档导航
├── tests/                    # 单元测试与集成测试用例
├── scripts/                  # 辅助脚本：批量导入、格式校验
├── dist/                     # 构建输出目录（生产静态文件）
├── Dockerfile                # 容器化部署定义
├── docker-compose.yml        # 本地组合服务示例
├── package.json              # 项目依赖与脚本入口
├── README.md                 # 项目总览（当前文件）
└── LICENSE                   # MIT 许可协议
```

## 贡献指南

欢迎社区提交改进建议或补充资源地址，请遵循以下流程：

1. 在 GitHub 仓库中 Fork 本项目，并创建以 `feature/` 或 `fix/` 为前缀的特性分支。
2. 修改 `config/resources.json` 文件时，请保持原有地址顺序，新增地址需注明来源或业务含义，并在提交信息中说明变更原因。
3. 若涉及探测逻辑或渲染模板的改动，请同步更新 `tests/` 目录下的对应测试用例，确保 `npm test` 全部通过。
4. 提交 Pull Request 前，请执行 `npm run lint` 检查代码风格，并确保 `README.md` 中的资源列表与实际配置文件保持同步。
5. PR 描述中需明确说明变更的影响范围，是否涉及破坏性改动，以及建议的版本号增量（patch/minor/major）。

## 常见问题

**Q：资源列表中的地址无法访问，项目会如何处理？**

A：项目本身不会自动移除或替换不可达地址，仅通过探测模块记录状态码与响应时间。用户可通过管理界面或 API 查看当前健康状态，并手动切换至备用地址。建议运维侧配置定期任务调用探测接口，将结果上报至监控系统。

**Q：为什么项目不自动补全 URL 的协议或 www 前缀？**

A：不同业务源对协议与子域名的处理逻辑存在差异（例如部分源仅支持 HTTP，部分强制 HTTPS），自动补全可能导致访问失败或证书错误。项目坚持原始地址透传原则，由调用方或上层网关根据实际策略进行处理。

**Q：如何在内网完全隔离的环境中使用本项目？**

A：本项目本身无外部依赖（除 Node.js 运行环境外），但资源列表中的地址需由内网用户自行判断可达性。可将 `config/resources.json` 替换为内网可访问的地址集合，并在 `probe.config.js` 中关闭外网探测或配置代理。

## 许可证

MIT

> 外链数量: 7 | 生成时间: 2026-08-14 22:01:34
