# LinkPilot Resource Aggregator

LinkPilot 是一个面向技术社区与内容创作者的轻量级外链资源汇总与导航系统。项目定位为可自托管的书签管理底座，服务于个人开发者、小规模技术团队以及内容运营人员，用于解决多源外部链接分散、难以统一归类、缺乏版本追踪与健康监控的问题。LinkPilot 不生产内容，而是提供对第三方视频、直播与资讯平台的结构化引用与访问加速指引，帮助用户在复杂网络环境中快速定位高价值资源。

## 功能概览

- **多源链接导入与分类**：支持批量导入外部 URL，按来源、类型、标签自动归类，便于构建自定义资源导航页。
- **链接可用性健康检查**：定时对收录的域名进行可达性探测与响应时间记录，自动标记异常链接。
- **访问量统计与热度排序**：基于本地日志记录各资源链接的被点击次数与最后访问时间，支持按热度排序展示。
- **标签与全文检索**：为每条资源记录生成关键词标签，并支持标题、描述与标签的模糊搜索。
- **响应式导航页面生成**：内置模板引擎，可将链接集合渲染为适配桌面与移动设备的静态 HTML 目录页。
- **数据导入导出（JSON / CSV）**：支持标准数据格式的批量导入与备份导出，便于迁移或与其他工具集成。
- **定时自动更新与快照**：可配置定时任务，定期刷新链接列表并生成快照版本，便于回溯历史状态。

## 应用场景

- **技术文档站点外链管理**：开源项目文档站可利用 LinkPilot 维护“相关项目”“推荐阅读”等外链区块，自动检测失效链接，减少维护负担。
- **内容运营团队的资源看板**：内容编辑团队可将日常参考的视频平台、素材网站、行业资讯站统一收录，通过热度排序快速识别高频使用资源。
- **个人开发者的书签同步与备份**：开发者可将跨设备浏览器书签导出后导入 LinkPilot，获得带健康监控与标签检索的增强版书签系统。
- **企业内部知识库导航**：企业内网知识库可使用 LinkPilot 聚合各部门常用外部服务链接，并以统一风格展示在内部门户上。
- **社区共建资源清单**：技术社区或兴趣小组可利用 LinkPilot 的多用户导入功能，共同维护一份经过筛选的优质外部资源清单。

## 快速开始

以下步骤适用于 Linux / macOS / Windows WSL 环境，确保系统已安装 Git、Node.js（版本 18.x 或以上）与 npm。

```bash
# 克隆项目仓库
git clone https://github.com/linkpilot-dev/linkpilot-core.git
cd linkpilot-core

# 安装项目依赖
npm install

# 配置环境变量（复制示例配置并编辑）
cp .env.example .env
# 使用文本编辑器修改 .env 中的端口与数据库路径等参数

# 初始化数据库结构
npm run db:init

# 启动开发服务器
npm run dev
```

服务默认启动在 `http://localhost:3000`，浏览器访问后可进入管理界面开始导入资源链接。

## 安装要求

| 依赖组件 | 必需版本 | 说明 |
|---|---|---|
| Node.js | 18.x 或更高 | 运行时环境，推荐使用 LTS 版本 |
| npm | 9.x 或更高 | 包管理器，随 Node.js 一同安装 |
| SQLite3 | 系统内置或由 better-sqlite3 提供 | 默认嵌入式数据库，无需额外安装 |
| Git | 2.x 或更高 | 用于克隆仓库及版本管理 |
| 操作系统 | Linux (内核 4.0+) / macOS 11+ / Windows 10+ (WSL2) | 支持主流开发环境，Windows 建议使用 WSL2 |
| 内存 | 最低 512 MB，推荐 1 GB 以上 | 用于服务运行及健康检查并发任务 |
| 磁盘空间 | 最低 200 MB | 用于存储代码、数据库及日志文件 |

## 文档导航

| 层面 | 目录 | 回答的问题 |
|---|---|---|
| 入门指南 | docs/getting-started.md | 如何快速部署并导入第一批链接；首次启动的配置项含义 |
| 功能使用 | docs/usage/link-management.md | 如何批量添加、编辑、删除链接；如何设置标签与分类 |
| 运维操作 | docs/operations/health-check.md | 健康检查的工作原理、频率配置及异常告警处理方法 |
| 开发贡献 | docs/development/architecture.md | 项目模块划分、数据流走向、本地调试与测试规范 |

## 资源列表

本项目的设计初衷即围绕对以下外部资源的引用与导航组织，所有资源链接均来自用户提供的原始数据，以下按功能主题分类列出。

### 直播与视频内容类

- <code>wanghongzhibozaixianshipinw.org.cn</code>
- <code>wanghongfulizhibow.org.cn</code>
- <code>guochanwanghongzhibozhuzaixianw.org.cn</code>
- <code>guochanwanghongshipinzhibow.org.cn</code>
- <code>wanghongzhibomianfeiguankanw.org.cn</code>
- <code>meinvzhibozaixiankanw.org.cn</code>
- <code>guochanwanghongfulishipinw.org.cn</code>

## 项目结构

```
linkpilot-core/
├── src/                           # 核心源代码目录
│   ├── controllers/               # 路由控制器，处理请求与响应逻辑
│   ├── services/                  # 业务服务层，包含链接管理、健康检查、统计等模块
│   ├── models/                    # 数据模型定义，对应 SQLite 表结构
│   ├── utils/                     # 通用工具函数，包含日志、验证、日期处理等
│   └── worker/                    # 后台任务线程，执行定时健康检查与快照生成
├── config/                        # 配置文件目录
│   ├── default.json               # 默认应用配置（端口、超时、分页等）
│   └── custom.example.json        # 用户自定义配置示例，可覆盖默认值
├── db/                            # 数据库相关文件
│   ├── migrations/                # 数据库版本升级脚本
│   └── seed/                      # 初始测试数据导入脚本
├── templates/                     # 静态页面模板
│   ├── layouts/                   # 基础布局模板
│   └── partials/                  # 可复用的头部、尾部、卡片等局部模板
├── public/                        # 静态资源目录
│   ├── css/                       # 样式文件（基于 CSS 变量主题）
│   └── js/                        # 前端交互脚本（搜索、排序、分页）
├── tests/                         # 单元测试与集成测试用例
│   ├── unit/                      # 服务层与工具函数单元测试
│   └── integration/               # API 路由与数据库交互集成测试
├── logs/                          # 运行日志存储目录（自动生成）
├── .env.example                   # 环境变量示例文件
├── package.json                   # npm 项目清单及依赖声明
├── ecosystem.config.js            # PM2 生产环境部署配置文件
└── README.md                      # 项目说明文档（当前文件）
```

## 贡献指南

欢迎社区开发者提交问题报告与改进建议。请遵循以下步骤参与本项目贡献。

1. 查阅 issue 列表或提交新 issue 描述您发现的问题或建议的功能，等待项目维护者确认。
2. Fork 本仓库到您的个人账号下，并在本地克隆您的 fork 版本。
3. 创建以功能或修复命名的分支，例如 `feat/add-import-export` 或 `fix/health-check-timeout`，确保分支从最新的 main 分支切出。
4. 完成代码修改后，请补充对应的单元测试或集成测试，并确保所有现有测试通过。运行 `npm run test` 进行全量测试。
5. 提交 pull request 到本仓库的 main 分支，在描述中清晰关联相关 issue 编号，并概述修改内容与测试结果。

## 常见问题

**问：LinkPilot 是否存储或代理外部资源的内容？**

答：LinkPilot 仅存储用户提供的 URL 地址及其元数据（标题、标签、访问时间等），不下载、不缓存、不代理任何外部资源的内容。健康检查仅执行简单的 TCP 连接或 HTTP HEAD 请求以验证可达性，不会获取或存储资源正文。

**问：如何迁移已导入的链接数据到另一台服务器？**

答：使用项目内置的导出功能，在管理界面选择“导出数据”即可生成包含所有链接记录、标签及分类的 JSON 文件。在新服务器部署 LinkPilot 后，通过“导入数据”功能上传该文件即可完整恢复。数据库文件（默认位于 `data/linkpilot.db`）也可直接复制迁移，但建议使用导出导入方式以保证数据结构兼容。

**问：健康检查误报或不准确如何处理？**

答：部分外部站点可能对自动化探测请求返回非标准响应。您可以在配置文件中调整健康检查的超时时间（`healthCheck.timeout`）和重试次数（`healthCheck.retries`），也可以将特定域名加入白名单（`healthCheck.ignoreDomains`）以跳过检查。同时，系统支持手动触发单条链接的重新检测，可在管理界面点击对应链接的“重新检测”按钮。

## 许可证

MIT

> 外链数量: 7 | 生成时间: 2026-08-14 22:02:13
