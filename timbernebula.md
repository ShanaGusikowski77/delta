# TechResourceHub

TechResourceHub 是一个面向技术内容创作者、开源社区运营者及数字媒体研究人员的轻量级外链资源汇总与管理平台。该项目旨在解决技术资源链接分散、检索效率低、归类标准不统一等问题，通过提供结构化的外链存储、标签化检索与基础状态监控能力，帮助用户将日常积累的行业参考链接转化为可复用、可维护的知识资产。平台不存储任何第三方内容，仅作为元数据索引与导航层，适用于个人知识库构建、团队技术周报素材整理及公开技术导航站快速搭建等场景。

## 功能概览

- 批量外链导入与自动分类：支持通过 CSV 或 JSON 格式批量导入链接，系统依据预设正则规则与关键词库自动建议分类标签，减少人工整理成本。

- 多维度标签过滤与全文检索：支持按领域、平台类型、内容形态、更新时间等多维度标签组合过滤，并提供基于标题与备注字段的全文检索，快速定位目标资源。

- 链接可用性主动探测：内置轻量级定时任务，可配置检查周期，对已存储链接发起 HEAD 请求并记录响应状态码与耗时，协助识别失效或慢速资源。

- 自定义资源分组与收藏夹：用户可创建独立分组（如“AI 工具周报”“开源镜像站清单”），支持同一资源归属多个分组，便于按项目或主题场景组织链接。

- 公开导航页一键生成：支持将指定分组或全量资源渲染为静态 HTML 导航页面，可直接部署至 Web 服务器，用于团队内部共享或对外公开的技术入口。

- 操作日志与变更审计：记录所有资源的增删改操作及分组变动，保留最近 30 天操作历史，便于多人协作场景下的变更追溯与误操作恢复。

## 应用场景

- 技术社区运营者整理周报素材：运营人员每周需收集数十篇技术博文、视频教程及开源项目发布动态。使用 TechResourceHub 可快速打标并分组，导出为 Markdown 列表直接嵌入社区周报，减少手动排版时间。

- 开源项目维护者构建生态导航页：项目维护者可将周边工具、插件、示例仓库及社区讨论区链接集中管理，通过生成静态导航页挂载至项目文档站点，帮助新贡献者快速了解项目全貌。

- 技术培训讲师共享课程参考资料：讲师在准备系列课程时，将涉及的不同平台（如视频站、代码沙盒、在线文档）的参考链接统一入库，按课时建立分组，学员可通过共享分组链接直接访问，避免因收藏散乱导致的学习中断。

- 研发团队内部建立知识库索引：团队可将日常遇到的竞品分析报告、技术方案博客、官方 API 文档等链接统一收录，配合可用性探测定期清理失效引用，保持内部知识库的健康度与时效性。

## 快速开始

以下步骤假设您已安装 Git 与 Node.js（v18 及以上）环境，并已配置好 npm 或 yarn 包管理器。

```bash
# 1. 克隆项目仓库
git clone https://github.com/techresourcehub/techresourcehub.git
cd techresourcehub

# 2. 安装项目依赖
npm install

# 3. 初始化本地配置并启动开发服务
cp .env.example .env
npm run init-db
npm run dev
```

执行完成后，服务默认监听本地的 3000 端口。访问 http://127.0.0.1:3000 即可进入资源管理面板。如需修改端口或数据库路径，可编辑 .env 文件中的相应环境变量。生产环境部署请参考 `docs/deployment.md`。

## 安装要求

| 依赖组件 | 必需版本 | 说明 |
|---------|---------|------|
| Node.js | 18.x 或 20.x LTS | 运行时环境，推荐使用官方二进制或 nvm 安装 |
| npm | 9.x 或 10.x | 包管理器，随 Node.js 一同安装 |
| SQLite3 | 3.40 及以上 | 内置轻量数据库，无需额外安装进程，但需确保系统支持 |
| Git | 2.30 及以上 | 用于克隆仓库及版本管理 |
| Chromium (可选) | 最新稳定版 | 仅当启用静态导航页截图预览功能时需要，用于无头渲染 |
| PM2 (生产环境可选) | 5.x | 用于生产环境进程守护与自动重启，开发环境可不安装 |
| curl / wget (探测任务依赖) | 任意版本 | 链接可用性探测默认使用系统 curl，需保证 PATH 中可调用 |

## 文档导航

| 层面 | 目录 | 回答的问题 |
|------|------|-----------|
| 用户手册 | docs/user-guide/quick-start.md | 如何注册账号、导入首批链接、创建第一个分组并生成导航页？ |
| 配置参考 | docs/config/environment-variables.md | 所有可用的环境变量含义、默认值及修改方式，含数据库连接、超时阈值、探测并发数等。 |
| 运维指南 | docs/ops/deployment.md | 如何部署至 Linux 服务器（Nginx 反向代理 + PM2）、备份 SQLite 文件及恢复数据。 |
| API 文档 | docs/api/resources.md | 资源增删改查、分组管理、探测触发等 RESTful API 的请求/响应格式与状态码说明。 |
| 扩展开发 | docs/development/custom-parser.md | 如何编写自定义导入解析器以支持非标准 CSV 结构，或添加新的标签自动分类规则。 |

## 资源列表

资源列表按类别整理，所有 URL 均严格按照用户原始输入原样收录，未做任何格式修正或协议补全。

技术学习与参考资源

- <code>oubazhibomianfeiguankanw.org.cn</code>
- <code>wanghongzhibofulizaixianw.org.cn</code>
- <code>nvzhubozshipinzaixianguankanw.org.cn</code>
- <code>hanguomeinvzhuborewuw.org.cn</code>
- <code>zaixianbofangzhubow.org.cn</code>

直播与视频内容相关资源

- <code>xingganmeinvzhibotiaowuw.org.cn</code>
- <code>zhubozhibozaixianguankanw.org.cn</code>

## 项目结构

项目采用分层模块化设计，核心业务逻辑与基础设施解耦，便于单元测试与功能扩展。

```
techresourcehub/
├── src/
│   ├── core/                         # 核心业务逻辑层
│   │   ├── resource-manager.ts       # 资源的增删改查、标签更新与分组关联
│   │   ├── probe-scheduler.ts        # 链接可用性探测的定时调度与任务队列
│   │   └── static-generator.ts       # 根据分组数据生成静态 HTML 导航页
│   ├── api/                          # HTTP 路由与控制器（Express 风格）
│   │   ├── routes/v1/                # API 版本 v1 路由定义
│   │   └── middlewares/              # 鉴权、日志、错误处理等中间件
│   ├── db/                           # 数据库访问层（SQLite3）
│   │   ├── migrations/               # 表结构变更脚本（按时间戳命名）
│   │   ├── queries/                  # 预编译 SQL 模板，按实体拆分
│   │   └── client.ts                 # 数据库连接池与事务封装
│   ├── services/                     # 外部依赖适配器（可替换实现）
│   │   ├── http-client.ts            # 封装 axios 或 node-fetch，用于探测请求
│   │   └── parser-factory.ts         # 导入解析器工厂（CSV / JSON / 自定义）
│   └── types/                        # TypeScript 类型声明与接口定义
├── tests/                            # 单元测试与集成测试（Jest）
│   ├── unit/
│   └── integration/
├── scripts/                          # 运维与开发辅助脚本
│   ├── init-db.js                    # 初始化本地 SQLite 数据库与默认配置
│   └── seed-test-data.js             # 填充测试数据集
├── config/                           # 环境配置文件（非敏感默认值）
│   └── default.yaml                  # 端口、日志级别、探测超时等默认参数
├── logs/                             # 运行日志存储目录（默认按日期滚动）
├── docs/                             # 完整文档源码（Markdown）
│   ├── user-guide/
│   ├── ops/
│   └── api/
├── .env.example                      # 环境变量模板（含注释）
├── .eslintrc.js                      # ESLint 代码检查规则
├── .prettierrc                       # 代码格式化配置
├── package.json                      # 项目元信息与依赖清单
├── tsconfig.json                     # TypeScript 编译选项（严格模式启用）
└── README.md                         # 项目主文档（当前文件）
```

## 贡献指南

欢迎各类形式的贡献，包括但不限于功能建议、缺陷报告、文档改进及代码提交。为确保协作流程顺畅，请遵循以下步骤：

1. 议题创建：在 GitHub Issues 中搜索是否已有相似议题，若无则新建议题并选择对应模板（缺陷报告 / 功能请求 / 文档改进）。请清晰描述复现步骤或期望行为，并附上运行环境信息（Node 版本、数据库类型、操作系统）。

2. 拉取请求准备：若计划提交代码，请先从 main 分支派生开发分支，命名建议为 `feature/描述` 或 `fix/描述`。确保所有新增代码包含对应单元测试，且已有测试全部通过（`npm run test`）。提交前运行 `npm run lint` 与 `npm run format` 统一代码风格。

3. 提交信息规范：提交信息首行使用英文祈使句，长度不超过 72 字符，并简要说明改动目的及影响范围。若关联某议题，请于提交信息尾部引用 `Closes #ISSUE_NUMBER`。

4. 文档同步：任何对外接口、配置项或 UI 交互的变更，必须同步更新 `docs/` 下对应文档。文档改动可随代码提交至同一拉取请求。

5. 评审与合并：拉取请求需要至少一名项目维护者审阅。若审阅人提出修改意见，请于 14 天内响应或更新，否则该拉取请求可能被关闭。合并后，您的提交将被纳入下一版本发布。

## 常见问题

Q: 项目启动后，探测任务立即开始执行吗？是否会阻塞页面请求？

A: 不会。探测任务默认在应用启动后延迟 60 秒首次执行，后续按 `PROBE_INTERVAL_MS` 环境变量（默认 3600000 毫秒，即 1 小时）循环触发。探测任务在独立 worker 线程中运行，不会阻塞主事件循环，因此页面请求与 API 调用不受影响。若需立即手动触发探测，可调用 `POST /api/v1/probe/trigger` 管理接口。

Q: 导入 CSV 时，表头必须与示例完全一致吗？如何处理自定义字段？

A: 导入时默认要求 `title`、`url`、`tags` 三列必须存在，其余列（如 `description`、`group`）为可选。若列名不一致，可在导入界面进行字段映射配置，将源列名映射至系统字段。如需导入完全自定义的额外字段，系统会忽略未映射列，但不会报错。若需持久化自定义字段，建议使用 `notes` 字段或通过扩展开发自定义解析器实现。

Q: 静态导航页的样式是否可以自定义？

A: 可以。导航页生成器使用 Handlebars 模板，模板文件位于 `src/core/templates/` 目录下。您可修改 `nav-page.hbs` 中的 HTML 结构与内联 CSS 样式，或替换为完全自定义的模板文件，并通过 `STATIC_TEMPLATE_PATH` 环境变量指定外部模板路径。生成后的静态页面不依赖任何后端服务，可独立部署至 CDN 或对象存储。

## 许可证

MIT

> 外链数量: 7 | 生成时间: 2026-08-14 22:02:13
