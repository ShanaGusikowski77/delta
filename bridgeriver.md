# HyperLink Navigator

HyperLink Navigator 是一个面向技术社区与终端用户的高质量网络资源导航与元数据检索系统。该项目定位于构建可维护、可扩展的网络外链聚合中间层，为开发者、内容研究者、运维人员以及信息分析从业者提供结构化的外部资源访问能力。项目不产生任何自有内容，仅专注于对第三方链接进行整理、分类、可用性监控与结构化呈现，解决用户在信息过载环境下对高价值网络资源“找得到、看得清、用得稳”的核心诉求。

通过标准化的目录树组织、自动化的状态检查插件以及轻量级的前端呈现方案，HyperLink Navigator 帮助团队或个人快速建立私有或公有的网络资源索引体系。项目本身不依赖数据库，所有资源清单以纯文本形式维护于代码仓库中，支持版本追踪、冲突合并与离线浏览，适用于文档站点底座、内部知识库外链模块以及个人书签管理系统的二次开发。

## 功能概览

- **结构化资源目录**：支持多级分类与标签体系，所有资源链接按主题、地域、语言或业务域进行编目，便于快速定位与批量维护。

- **链接可用性监测**：内置基于 HTTP 状态码的周期检测任务，可标记失效链接、重定向链过长链接以及响应超时链接，支持输出 JSON 格式报告。

- **纯静态站点生成**：基于 Markdown 源文件与轻量模板引擎，生成无服务端依赖的静态 HTML 导航页面，兼容 CDN 部署与内网发布。

- **元数据扩展字段**：每条资源可附加来源描述、语言属性、更新频率、国家或地区标识等自定义字段，满足垂直领域的信息细化需求。

- **变更历史审计**：所有资源新增、删除或链接变更操作均通过 Git 提交记录追溯，支持按时间范围回滚或审查修改原因。

- **多终端适配视图**：前端响应式布局针对桌面端与移动端分别优化，列表视图与卡片视图可切换，提升不同设备下的浏览体验。

- **对外 API 查询接口**：提供轻量级 HTTP API，支持按关键词、分类或状态过滤资源列表，便于第三方系统集成调用。

## 应用场景

- **技术文档站外链管理**：技术团队在维护项目文档或 API 手册时，需要引用大量外部规范、教程或工具站点。HyperLink Navigator 可作为独立的外链模块嵌入文档站点，统一管理所有外部引用，并自动检测链接有效性，避免文档中出现失效引用。

- **行业资讯聚合导航**：媒体编辑或行业分析师需每日浏览多个垂直领域的新闻源、数据平台与报告库。本系统可将这些分散的链接组织为分类导航页，配合静态生成能力发布为团队内部起始页，减少重复查找时间。

- **本地化内容研究辅助**：涉及多语言或多地区内容比对的研究人员，可利用项目中的地域与语言元数据字段快速筛选特定区域的资源集合，例如不同国家或地区的视频平台、字幕站点或文化内容库，提升跨地域信息采集效率。

- **个人书签系统重构**：长期积累的浏览器书签数量庞大且缺乏维护。使用 HyperLink Navigator 可将书签导出为统一格式的资源清单，借助目录树与监测能力系统化整理，并生成可分享的静态页面，替代浏览器自带的扁平化书签管理。

## 快速开始

以下步骤适用于 Linux 与 macOS 开发环境，Windows 用户建议使用 WSL2 或 Git Bash 执行。

```bash
# 1. 克隆项目仓库
git clone https://github.com/hyperlink-navigator/hln-core.git
cd hln-core

# 2. 安装依赖（项目基于 Node.js 18+ 与 npm）
npm install

# 3. 执行资源清单校验与静态站点生成
npm run build

# 4. 启动本地开发服务器（默认监听端口 8080）
npm run serve
```

执行完成后，打开浏览器访问 `http://localhost:8080` 即可查看生成的导航首页。资源清单源文件位于 `./data/catalog.yml`，修改后重新运行 `npm run build` 即可更新页面内容。

## 安装要求

| 依赖项 | 必需版本 | 说明 |
|--------|----------|------|
| Node.js | 18.x 或 20.x LTS | 运行时环境，用于执行构建脚本与 API 服务 |
| npm | 8.x 或 9.x | 包管理器，用于安装项目依赖 |
| Git | 2.25 及以上 | 版本控制工具，用于克隆仓库与提交变更 |
| YAML 解析器 | 项目内置 | 用于解析 `catalog.yml` 资源清单文件 |
| HTTP 客户端 | 项目内置 | 用于链接可用性检测，基于 Node.js `http` / `https` 模块 |
| 磁盘空间 | 至少 50 MB | 用于存放源码、依赖及生成的静态文件 |
| 内存 | 建议 512 MB 以上 | 用于构建过程及 API 服务运行（轻量级场景） |

## 文档导航

| 层面 | 目录 | 回答的问题 |
|------|------|------------|
| 入门指南 | `docs/quick-start.md` | 如何快速部署、配置第一个资源目录并生成页面 |
| 目录规范 | `docs/catalog-syntax.md` | 资源清单 YAML 文件的字段定义、分类命名规则与示例 |
| 监测运维 | `docs/health-check.md` | 如何配置周期检测、解读报告、处理失效链接 |
| API 参考 | `docs/api-reference.md` | 查询接口的端点、参数、返回格式及鉴权方式（若有） |
| 模板定制 | `docs/template-guide.md` | 如何修改前端主题、自定义列表布局与 CSS 变量 |
| 贡献流程 | `CONTRIBUTING.md` | 外部贡献者如何提交资源新增、更新或删除请求 |

## 资源列表

本部分列出项目当前索引的所有外部网络资源。所有链接均按原始格式原样呈现，未做任何协议补全或域名规范化处理。

### 视频资源相关

- <code>mianfeigaoqingshipinzaixianguankanb.org.cn</code>
- <code>renqixiliezhongwenzimuwb.org.cn</code>
- <code>rihanmeinvzhongwenzimub.org.cn</code>
- <code>qingqingcaoyuanzhongwenzimub.org.cn</code>

### 直播与主播资源相关

- <code>wanghongzhibozaixianshipin.org.cn</code>
- <code>wanghongfulizhibo.org.cn</code>
- <code>guochanwanghongzhibozhuzaixian.org.cn</code>

## 项目结构

```
hln-core/
├── bin/                                 # 可执行脚本目录
│   ├── cli.js                           # 命令行入口，处理 build / serve / check 指令
│   └── health-worker.js                 # 独立运行的链接检测守护进程
├── config/                              # 项目配置文件目录
│   ├── app.config.yml                   # 站点标题、主题色、分页大小等全局配置
│   └── routes.yml                       # 静态页面路由映射，定义路径与模板的对应关系
├── data/                                # 数据目录（核心资源清单存放处）
│   ├── catalog.yml                      # 主资源索引文件，按分类组织所有外部链接
│   └── overrides/                       # 环境特定覆盖配置（如开发/生产不同检测策略）
├── docs/                                # 项目文档目录
│   ├── quick-start.md                   # 快速入门指南
│   ├── catalog-syntax.md                # 资源清单语法规范
│   ├── health-check.md                  # 健康检测配置与解读说明
│   └── api-reference.md                 # API 接口文档
├── src/                                 # 核心源码目录
│   ├── parser/                          # YAML 解析与校验模块
│   ├── generator/                       # 静态页面生成器，含模板渲染与输出写入
│   ├── api/                             # 轻量 HTTP API 服务实现
│   ├── monitor/                         # 链接可用性检测核心逻辑
│   └── utils/                           # 通用工具函数（日志、文件操作、网络请求）
├── templates/                           # 前端模板文件目录
│   ├── layouts/                         # 基础布局模板（头部、底部、导航栏）
│   └── partials/                        # 可复用组件（资源卡片、列表项、分页控件）
├── public/                              # 静态资源输出目录（构建后生成）
│   ├── index.html                       # 生成的首页
│   ├── assets/                          # CSS / JavaScript / 图片等静态资产
│   └── reports/                         # 链接检测报告 JSON 文件存放处
├── tests/                               # 单元测试与集成测试目录
│   ├── parser.test.js                   # YAML 解析模块测试
│   ├── monitor.test.js                  # 检测逻辑测试（含模拟网络响应）
│   └── fixtures/                        # 测试用固定数据样本
├── .gitignore                           # Git 忽略规则，排除 node_modules / public / logs
├── package.json                         # npm 包定义，含依赖与脚本命令
├── README.md                            # 项目主说明文档（即本文件）
└── LICENSE                              # MIT 许可证文本
```

## 贡献指南

我们欢迎社区提交资源新增、更新删除、文档改进以及代码优化类贡献。请遵循以下步骤确保协作流程顺畅：

1. **查阅现有议题与文档**：在提交变更前，请阅读 `docs/` 目录下的相关规范，并检查 GitHub Issues 中是否已有类似提议或正在进行的相关讨论，避免重复工作。

2. **分支开发与资源编辑**：从 `main` 分支创建新的特性分支（如 `feature/add-video-resources`）。所有资源清单变更仅操作 `data/catalog.yml` 文件，遵循 YAML 缩进与字段定义。对于新增链接，请确保填写完整的描述与分类标签。

3. **本地构建与自测**：提交前在本地运行 `npm run build` 确保构建通过，并执行 `npm run check` 触发链接检测，确认新增或变更的链接处于可访问状态。若链接失效，请先与源站确认后再提交。

4. **提交变更说明**：提交信息应使用清晰的中文或英文描述，格式为 `<类型>: <简短说明>`，例如 `feat: 新增视频分类下四个资源链接` 或 `fix: 修正直播分类中失效的域名`。提交信息应包含变更动机与影响范围。

5. **发起合并请求**：向 `main` 分支发起 Pull Request，并在描述中列出变更内容、检测结果截图或日志摘要。至少一名项目维护者审核通过后即可合并。对于资源数量超过 10 条的大批量变更，建议先开议题讨论再操作。

## 常见问题

**问：资源清单中的链接访问失败，如何处理？**

项目内置的链接检测脚本会定期扫描 `catalog.yml` 中所有链接并生成报告。若发现特定链接连续三次检测均返回非 200 状态码或超时，系统会将其标记为 `unstable` 状态。用户可手动编辑 YAML 文件，为该链接添加 `status: suspect` 注释，或直接移除该条目。建议在移除前访问原始站点确认是否临时维护或永久关闭。

**问：生成静态页面后，如何部署到公网或内网？**

构建命令 `npm run build` 会在 `public/` 目录生成完全自包含的 HTML、CSS 与 JavaScript 文件。您可以将该目录整体上传至任意 Nginx、Apache 或 S3 静态托管服务。若需要部署到内网，直接复制 `public/` 到内网 Web 服务器根目录即可，无需额外配置后端。API 查询功能为独立服务，需通过 `npm run serve` 启动，若不需要 API，可仅部署静态文件。

**问：能否导入现有的浏览器书签文件？**

当前版本未提供直接的 HTML 书签导入解析器，但您可以将浏览器导出的书签 HTML 文件中的 `<a href="...">` 链接提取为 YAML 格式列表。社区贡献者正在开发基于 `puppeteer` 的辅助转换脚本，预计在后续版本中提供。现阶段，推荐使用在线书签转 YAML 工具或手动整理。

## 许可证

MIT

> 外链数量: 7 | 生成时间: 2026-08-14 22:01:34
