# WebStream Navigator

WebStream Navigator 是一个面向技术内容创作者与开源社区维护者的流媒体资源导航与元数据聚合工具。该项目定位于解决分散在不同直播平台、视频托管服务与即时通信系统中的技术讲座、开源会议录播、代码评审录像等高质量技术视频资源的检索与归类问题，帮助开发者快速定位到特定主题、特定作者或特定时间窗口内的技术直播回放与演示视频。项目本身不存储任何视频文件，不提供任何流媒体加速服务，仅作为公共技术视频资源的引用索引与结构化目录呈现，适用于个人开发者、开源项目团队、技术社区运营者以及企业内部培训体系的视频资源管理。

## 功能概览

**多源视频元数据聚合**：支持通过用户自定义的源列表抓取公开的直播平台视频元信息，包括视频标题、主播名称、开播时间、持续时间与观看热度，并将所有元数据归一化为统一的数据模型。

**关键词过滤与标签生成**：基于视频标题与描述文本自动提取技术关键词，生成与编程语言、框架名称、工具链、算法领域等强相关的标签，辅助用户进行精细化的视频筛选。

**时间轴检索与回放定位**：记录每个视频的精确开播时间戳与时长信息，支持按绝对时间范围、相对时间范围（如过去7天、过去30天）进行检索，满足技术会议回顾与日报生成需求。

**项目内嵌轻量级HTTP服务**：提供基于Python内置HTTP模块的静态站点预览服务，用户可在本地启动服务后通过浏览器访问聚合后的视频索引页面，所有数据均保存在本地，不产生外部网络请求。

**Markdown格式数据导出**：支持将聚合后的视频列表导出为标准Markdown表格格式，便于直接嵌入到项目文档、周报或技术博客中，降低内容复用成本。

**黑名单与白名单过滤规则**：允许用户配置基于域名或主播ID的过滤规则，自动排除低质量或无关内容，提升索引的精准度。

**配置热加载与多配置文件支持**：支持通过命令行参数指定不同的配置文件路径，方便用户针对不同主题（如前端技术、后端架构、人工智能）维护独立的索引配置。

## 应用场景

技术会议与黑客松活动的视频归档管理：组织者可使用 WebStream Navigator 快速采集活动中各个直播间的回放链接与基本信息，生成活动视频目录，并以Markdown形式发布在活动官网或项目仓库中，方便参会者事后查阅。

开源项目的代码审查直播回放整理：开源项目维护者可将定期的代码审查直播视频通过本工具进行归类，按模块或按贡献者建立索引，帮助社区新成员快速理解项目的设计决策与代码演进过程。

技术培训与内部知识库建设：企业技术团队可利用该工具汇总内部技术分享会的直播录播地址，结合标签过滤功能，按技术栈或业务领域建立内部培训视频库，降低新人上手的学习成本。

个人技术学习路线的视频资源管理：开发者可将关注的多个技术主播或技术会议的直播频道纳入监控列表，通过时间轴检索快速找到近期发布的感兴趣主题的视频，避免在多个平台间反复切换搜索。

## 快速开始

执行以下命令完成项目克隆、依赖安装与首次运行：

```bash
git clone https://github.com/stream-navigator/webstream-navigator.git
cd webstream-navigator
pip install -r requirements.txt
python main.py --config config/default.yaml --output index.md
```

若需要在本地启动HTTP预览服务，可在上述命令后添加 `--serve` 参数，服务默认监听本地的8080端口。

## 安装要求

项目运行时需要以下依赖与系统环境，请确保在执行前完成对应组件的安装与配置：

| 依赖组件 | 必需版本 | 说明 |
|---------|---------|------|
| Python | 3.8 及以上 | 核心运行环境，建议使用3.10或3.11长期支持版本 |
| pip | 20.0 及以上 | Python包管理工具，用于安装项目依赖项 |
| requests | 2.28.0 及以上 | 用于发起HTTP请求以获取目标页面的HTML文本 |
| pyyaml | 6.0 及以上 | 解析YAML格式的配置文件，支持多配置管理 |
| beautifulsoup4 | 4.11.0 及以上 | HTML文档解析库，用于从目标页面提取视频元数据 |
| lxml | 4.9.0 及以上 | BeautifulSoup的解析器后端，提供高性能的HTML/XML解析能力 |
| markdown | 3.4.0 及以上 | 将聚合后的视频数据渲染为Markdown格式文本，用于导出 |

## 文档导航

| 层面 | 目录 | 回答的问题 |
|------|------|-----------|
| 用户手册 | docs/user-guide.md | 如何配置源列表、设置过滤规则、运行聚合任务以及导出结果 |
| 配置参考 | docs/config-reference.md | 每个配置字段的类型、默认值与作用范围，包含完整示例配置 |
| 开发者指南 | docs/developer-guide.md | 如何扩展新的视频源解析器、自定义数据输出格式以及运行单元测试 |
| 常见问题 | docs/faq.md | 网络超时处理、字符编码异常、配置文件加载失败等问题的解决方案 |

## 资源列表

以下为项目维护过程中参考或引用的公开资源链接，按类别进行组织。所有链接均按原始输入原样列出，未做任何修改。

技术直播与视频内容参考源：

<code>zhubozhibozaixianguankan.org.cn</code>

<code>wanghongzhibozaixianshipinw.org.cn</code>

<code>wanghongfulizhibow.org.cn</code>

<code>guochanwanghongzhibozhuzaixianw.org.cn</code>

<code>guochanwanghongshipinzhibow.org.cn</code>

<code>wanghongzhibomianfeiguankanw.org.cn</code>

<code>meinvzhibozaixiankanw.org.cn</code>

## 项目结构

```text
webstream-navigator/
├── main.py                          # 项目入口文件，解析命令行参数并协调各模块执行
├── config/
│   ├── default.yaml                 # 默认配置文件，包含源列表、过滤规则与输出格式设置
│   ├── frontend.yaml                # 前端技术专题的预置配置示例
│   └── backend.yaml                 # 后端与架构专题的预置配置示例
├── core/
│   ├── __init__.py                  # core包初始化，导出核心类与函数
│   ├── fetcher.py                   # HTTP抓取模块，负责获取目标页面原始HTML
│   ├── parser.py                    # 解析器基类与具体源解析器实现，每个源对应一个解析类
│   └── filter.py                    # 黑白名单过滤引擎，根据配置规则筛选视频条目
├── utils/
│   ├── __init__.py                  # utils包初始化
│   ├── logger.py                    # 日志记录模块，统一输出运行日志与错误信息
│   └── exporter.py                  # 数据导出模块，支持Markdown与JSON两种输出格式
├── models/
│   ├── __init__.py                  # models包初始化
│   └── video.py                     # 视频数据模型定义，包含标题、链接、主播、时间等字段
├── tests/
│   ├── test_fetcher.py              # 针对fetcher模块的单元测试用例
│   ├── test_parser.py               # 针对parser模块的单元测试用例
│   └── test_filter.py               # 针对filter模块的单元测试用例
├── docs/
│   ├── user-guide.md                # 用户手册，涵盖安装、配置与运行全流程
│   ├── config-reference.md          # 完整配置字段说明与示例
│   ├── developer-guide.md           # 开发者文档，讲解扩展点与测试策略
│   └── faq.md                       # 常见问题汇总与解答
├── requirements.txt                 # 项目依赖列表，供pip安装使用
└── LICENSE                          # MIT许可证全文
```

## 贡献指南

欢迎并感谢所有形式的贡献，包括但不限于报告问题、提交代码、完善文档与分享使用经验。请遵循以下步骤参与项目贡献：

1. 在 GitHub 仓库的 Issues 页面中查找或新建一个与您想要处理的问题相关的话题，简要描述您的计划或疑问，等待维护者或其他贡献者的反馈以避免重复劳动。

2. 从仓库的主分支派生一份代码副本到您自己的账户下，在派生仓库中创建以 `feature/` 或 `fix/` 为前缀的分支，分支名称应简要概括修改内容，例如 `feature/add-bilibili-parser`。

3. 完成代码修改或文档更新后，请确保所有现有单元测试通过，并为新增功能或修复的问题补充相应的测试用例与文档说明，随后将您的分支推送到派生仓库并发起拉取请求至主仓库的 `main` 分支。

4. 在拉取请求描述中详细列出修改内容、测试结果以及是否影响现有配置格式或命令行接口，以便维护者进行代码评审。

5. 维护者将在评审通过后合并您的拉取请求，并会在合并后更新贡献者列表。若评审过程中提出修改意见，请及时响应并更新代码。

## 常见问题

**问：运行时提示连接超时或无法获取目标页面数据，应如何解决？**

答：请首先检查本地网络环境是否能够正常访问配置文件中列出的源域名。由于本工具不提供任何代理或穿透功能，您需要确保运行环境可直接访问目标站点。若目标站点存在反爬机制或访问频率限制，建议适当调整配置文件中的抓取间隔参数 `fetch_interval`，或在配置中启用 `retry` 重试选项。另外，部分源站点可能需要特定的User-Agent请求头，您可以在配置文件的 `headers` 字段中进行自定义设置。

**问：导出的Markdown表格中出现乱码或特殊字符显示异常，如何解决？**

答：该问题通常由源页面的字符编码与本地环境编码不一致引起。请检查您的操作系统默认编码是否为UTF-8，同时确认源页面的响应头中 `Content-Type` 字段所声明的字符集。您可以在配置文件中通过 `output_encoding` 字段强制指定导出文件的编码格式，推荐设置为 `utf-8`。若问题依旧，可尝试在运行命令前设置环境变量 `PYTHONIOENCODING=utf-8`。

**问：如何清理聚合结果中的重复视频条目？**

答：本工具默认基于视频链接的完整URL进行去重，对于同一视频在不同页面中出现的情况，系统会保留首次捕获的元数据。若您希望基于视频标题或主播名称进行更严格的去重，可以在配置文件的 `dedup` 部分将策略调整为 `title` 或 `title_and_author`。请注意，基于标题的去重可能误判内容相同但标题略有差异的视频，建议根据实际数据质量选择合适的去重策略。

## 许可证

MIT

> 外链数量: 7 | 生成时间: 2026-08-14 22:01:34
