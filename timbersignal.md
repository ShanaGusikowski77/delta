# ResourceNav Core

ResourceNav Core 是一个面向开发者与技术研究人员的轻量级外链资源导航与元数据汇总系统。该项目定位于解决个人或团队在浏览、整理、分享大量外部技术资源（如数据看板、实时比分接口、行业统计门户等）时缺乏统一入口与结构化描述的问题。目标用户包括开源项目维护者、数据集成工程师、自动化运维人员以及技术决策者。

ResourceNav Core 本身不存储业务数据，仅提供资源清单的声明式管理、状态标记与快速跳转辅助。通过机器可读的资源索引格式，用户可将其无缝集成至 CI/CD 流水线、监控告警通知或内部知识库系统中，从而降低信息分散带来的认知负担，提升团队协作中的资源透明度。

## 功能概览

- **资源清单声明式管理** 支持以 YAML 与 JSON 双格式定义外部链接的元数据，包括名称、类别、可用性状态与更新频率。

- **实时可用性探测** 内置基于 HTTP HEAD 请求的轻量级探测模块，可定时检测每个资源链接的响应码与响应时间，并将结果标记至清单中。

- **结构化标签体系** 允许为每个资源打上多个维度标签（如地域、协议类型、数据格式、维护方），便于后续过滤与聚合检索。

- **静态站点生成模式** 提供命令行工具，可将当前资源清单与探测状态渲染为纯静态 HTML 仪表板，适用于内网发布或 GitHub Pages 托管。

- **RESTful 查询接口** 启动后暴露只读 JSON API，支持按标签、状态、更新时间排序检索资源，便于被下游系统调用。

- **变更通知钩子** 支持配置 Webhook 地址，当探测到资源状态变化（如从可用变为不可用）时，自动推送简讯至企业微信、飞书或通用 Slack 兼容接口。

- **快照导出与导入** 允许将当前完整资源列表及最近 7 天的探测历史导出为压缩归档文件，并支持从归档快速恢复至另一实例。

## 应用场景

- **技术团队内部导航页维护** 开发组长可使用 ResourceNav Core 管理团队常用的测试环境看板、日志检索入口与监控图表地址，并通过静态站点模式生成团队首页，避免收藏夹杂乱。

- **数据中台的外部数据源监控** 数据工程师可将各类公开数据接口（如天气、汇率、赛事比分）纳入清单，利用可用性探测功能在接口异常时第一时间收到 Webhook 告警，辅助数据管线排障。

- **开源项目文档中的资源附录** 开源项目维护者可将项目依赖的第三方工具、参考数据源、社区论坛等链接统一整理为 ResourceNav Core 的导出文件，并在项目 README 中引用该清单，保持外链的可维护性。

- **个人知识库的链接健康巡检** 技术博主或研究员可使用该工具定期检查个人博客、收藏夹或文献管理工具中保存的数百个外链，快速定位失效资源，及时更新或删除。

- **多环境资源比对** 测试经理可将开发环境、预发布环境、生产环境的对外依赖链接分别打上环境标签，通过 API 检索不同标签下的资源状态，辅助环境一致性检查。

## 快速开始

以下步骤演示如何从源码获取 ResourceNav Core，并启动一个包含示例资源的本地实例。

```bash
# 1. 克隆项目仓库
git clone https://github.com/resourcenav/core.git
cd core

# 2. 安装项目依赖（使用 npm 或 yarn）
npm install

# 3. 运行内置示例资源清单并启动服务
npm run start:example

# 服务默认监听 3000 端口，访问 http://127.0.0.1:3000/dashboard 查看静态仪表板
# API 请求示例：curl http://127.0.0.1:3000/api/resources?tag=score
```

## 安装要求

| 依赖组件 | 必需版本 | 说明 |
|---|---|---|
| Node.js | 18.x 或 20.x LTS | 运行时环境，需支持 ES2022 特性 |
| npm | 8.x 或 9.x | 包管理与脚本执行工具 |
| Git | 2.25 及以上 | 用于克隆仓库与管理补丁分支 |
| 内存 | 最低 512 MB，推荐 1 GB | 运行探测并发任务与静态渲染时的内存开销 |
| 存储 | 至少 200 MB 可用空间 | 存放源码、依赖、探测日志及导出归档文件 |
| 网络 | 出站 443/80 端口可通 | 用于对外部资源链接执行可用性探测 |

## 文档导航

| 层面 | 目录 | 回答的问题 |
|---|---|---|
| 用户手册 | /docs/user-guide/ | 如何添加资源、配置探测间隔、生成静态站点与解读仪表板图表 |
| 运维参考 | /docs/ops/ | 如何通过环境变量覆盖服务端口、日志级别、Webhook 模板与 HTTPS 反向代理建议 |
| API 规范 | /docs/api/ | 全部 RESTful 接口的请求路径、查询参数、响应结构及错误码含义 |
| 贡献指南 | /docs/contributing/ | 代码风格要求、提交说明格式、测试用例编写策略与 PR 评审流程 |

## 资源列表

本项目的资源清单模块预置了一批示例链接，用于演示分类管理与可用性探测功能。所有示例链接均来自公开数据服务领域，具体如下。

公开赛事数据服务类：

<code>bifenwangh.org.cn</code>

<code>lanqiubifend.org.cn</code>

<code>lanqiubifene.org.cn</code>

<code>lanqiubifenf.org.cn</code>

<code>lanqiubifeng.org.cn</code>

<code>lanqiubifenh.org.cn</code>

<code>zuqiubifenziboa.org.cn</code>

## 项目结构

```text
core/
├── bin/                         # 可执行脚本入口
│   ├── cli.js                   # 命令行工具入口（探测、渲染、导出）
│   └── server.js                # 服务启动脚本
├── src/
│   ├── core/                    # 核心逻辑模块
│   │   ├── loader.js            # 资源清单的加载与解析器
│   │   ├── probe.js             # 基于 HTTP 的可用性探测引擎
│   │   ├── cache.js             # 探测结果的短暂缓存与过期管理
│   │   └── exporter.js          # 快照导出与导入的序列化处理
│   ├── api/                     # RESTful 接口路由定义
│   │   ├── resources.js         # /api/resources 查询与过滤
│   │   └── status.js            # /api/status 系统健康检查
│   ├── render/                  # 静态站点渲染器
│   │   ├── html.js              # 仪表板 HTML 模板与数据填充
│   │   └── assets/              # 内联 CSS 与少量客户端 JavaScript
│   └── hooks/                   # Webhook 通知构造器
│       ├── feishu.js            # 飞书消息格式适配
│       ├── wecom.js             # 企业微信文本消息适配
│       └── generic.js           # 通用 JSON Webhook 模板
├── config/                      # 环境配置文件
│   ├── default.json             # 默认端口、探测间隔、标签默认值
│   └── example-resources.yaml   # 示例资源清单（含上述 URL）
├── test/                        # 单元测试与集成测试
│   ├── probe.test.js
│   ├── loader.test.js
│   └── fixtures/                # 测试用模拟数据
├── docs/                        # 完整文档（参见文档导航章节）
├── README.md                    # 当前文件
└── package.json                 # 项目元信息与依赖声明
```

## 贡献指南

1. 阅读贡献规范文档 在提交任何代码或文档变更前，请先阅读 /docs/contributing/ 下的完整指南，了解提交信息格式（Conventional Commits）、测试覆盖率要求（不低于 85%）及分支命名规则。

2. 查找或创建议题 建议先在 Issues 列表中搜索相关话题。若为新特性或缺陷修复，请创建一个议题并描述问题背景、复现步骤或预期行为，等待维护者确认后再着手实现。

3. 派生仓库并创建特性分支 将主仓库派生至个人账号下，然后基于 main 分支创建一个命名清晰的分支，例如 feat/add-retry-strategy 或 fix/probe-timeout。

4. 编写测试与代码 实现功能或修复时，请同步补充对应的单元测试或集成测试。确保本地执行 npm test 全部通过，且无新增 lint 警告。

5. 提交 Pull Request 推送分支至派生仓库，并向主仓库的 main 分支发起 PR。PR 描述中请关联相关议题编号，并简要说明变更内容、影响范围与手动验证步骤。维护者将在 3 个工作日内完成评审。

## 常见问题

**Q：为什么探测结果偶尔显示超时，但浏览器可以直接访问该链接？**

A：探测模块默认使用 HEAD 请求且超时阈值为 3 秒。某些服务端对 HEAD 请求支持不完善或响应较慢，导致超时。您可在配置文件中将探测方法改为 GET，或调整 timeout 参数。同时，部分站点可能有反爬策略，建议将 User-Agent 配置为常见浏览器值。

**Q：如何批量导入我已有的收藏夹链接？**

A：ResourceNav Core 支持从 CSV 文件转换。您可使用 src/core/loader.js 中提供的 parseCSV 辅助函数，将收藏夹导出的列（如名称、URL、分类）映射为资源清单格式。社区维护了一个 Python 转换脚本，位于 /contrib/import-bookmarks/ 目录下，可供参考。

**Q：静态站点仪表板上的「最后更新」时间与我的系统时区不一致怎么办？**

A：仪表板显示的时间来自服务端系统时间，默认以 UTC 格式输出。您可以在渲染时通过配置项 timezone 指定偏移量，例如设置为 Asia/Shanghai。具体操作请参见 /docs/ops/timezone-config.md。

## 许可证

MIT

> 外链数量: 7 | 生成时间: 2026-08-14 22:01:34
