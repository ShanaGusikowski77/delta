# Nova Index

Nova Index 是一个面向技术内容创作者、本地化团队与数字归档研究者的外链资源聚合系统。该项目不提供任何文件存储或流媒体服务，仅作为公开可用信息源的导航索引，通过结构化分类与长期可用性监测，帮助用户快速定位分布于不同域名下的主题化内容集合。项目适用于需要定期查阅多源异构资料、建立个人知识索引体系或进行网络公开资源分布研究的场景。

## 功能概览

- **多源链接分类索引**：支持按主题、语言、内容形式等维度对海量外链进行动态分类，每个条目均包含来源域名、主题标签与状态标记。
- **可用性轮询检测**：内置轻量级 HTTP 状态检查器，可定期验证索引链接的可达性，自动标记异常条目并生成离线报告。
- **自定义标签体系**：允许用户为每条链接添加自由标签，支持层级化标签结构，便于构建个性化分类视图。
- **全文元数据搜索**：基于链接标题、描述、标签与域名信息的全文检索引擎，支持模糊匹配与布尔查询。
- **导入与导出接口**：提供 JSON 与 CSV 格式的批量导入导出功能，支持从现有书签文件或表格数据快速迁移。
- **静态站点生成模式**：可将索引数据输出为纯静态 HTML 页面，适用于无后端环境的公开部署或个人知识库发布。
- **访问统计看板**：展示各分类下的链接数量、新增趋势与检测通过率，辅助用户评估资源分布状况。

## 应用场景

- **技术文档本地化研究**：本地化团队可通过本索引集中管理分布于多个域名的字幕文件参考站点，快速比较不同来源的术语翻译风格，提升多语言内容生产的一致性。
- **网络公开资源归档**：数字归档研究者可利用项目的定期检测功能追踪特定主题资源站点的可用性变化，为网络内容演变分析提供数据支撑。
- **个人知识库外链管理**：独立开发者或研究人员可将日常查阅的各类技术博客、规范文档、工具站统一纳入索引，通过标签系统构建符合个人认知体系的外链网络。
- **内容聚合站点原型验证**：项目初期需要验证内容分类与检索逻辑的产品团队，可借助 Nova Index 快速搭建链接级原型，无需实际存储内容即可测试导航交互流程。
- **合规性审查辅助**：法务或合规部门可通过分类视图快速定位特定域名的引用情况，便于进行来源审查与引用记录追溯。

## 快速开始

以下步骤适用于在本地环境中部署 Nova Index 开发实例。

```bash
# 克隆代码仓库
git clone https://github.com/novaindex/novaindex.git
cd novaindex

# 安装项目依赖
pip install -r requirements.txt

# 初始化本地配置与数据库
python scripts/init_config.py
python scripts/migrate_db.py

# 启动开发服务器
python app.py --host 127.0.0.1 --port 8080
```

启动成功后，访问 `http://127.0.0.1:8080` 即可进入索引管理界面。首次启动将自动生成示例分类与演示数据，您可通过管理面板清空并导入自有链接集合。

## 安装要求

| 依赖组件 | 必需版本 | 说明 |
|---|---|---|
| Python | 3.9 - 3.12 | 核心运行环境，建议使用 pyenv 或 conda 管理 |
| SQLite | 3.35.0+ | 内置轻量级数据库，用于存储索引条目与检测记录 |
| Redis | 7.0+ | 可选，用于提升轮询检测任务的队列处理性能 |
| Node.js | 18.0+ | 仅用于静态站点生成模块的前端资源构建 |
| Git | 2.25+ | 用于版本控制与仓库克隆操作 |
| curl | 7.68+ | 可用性检测底层依赖，需支持 HTTP/2 |
| make | 4.2+ | 辅助执行常用开发任务脚本 |

## 文档导航

| 层面 | 目录 | 回答的问题 |
|---|---|---|
| 用户手册 | /docs/user/quickstart.md | 如何配置分类、添加链接、执行检测与生成静态站点 |
| 开发指南 | /docs/developer/architecture.md | 项目的模块划分、数据流与扩展点设计 |
| API 参考 | /docs/api/endpoints.md | 管理后台及导入导出接口的请求与响应格式 |
| 运维手册 | /docs/operator/deployment.md | 生产环境部署方案、性能调优与备份恢复策略 |
| 设计文档 | /docs/design/tagging-system.md | 标签系统的层级模型、合并规则与查询语法 |

## 资源列表

以下条目由项目外部维护，Nova Index 仅提供索引引用，不对链接内容及可用性作任何担保。

主题分类 A

<code>rihanshipinmianfeizaixianguankan.org.cn</code>

<code>mianfeigaoqingshipinzaixianguankan.org.cn</code>

主题分类 B

<code>renqixiliezhongwenzimuw.org.cn</code>

<code>rihanmeinvzhongwenzimu.org.cn</code>

<code>qingqingcaoyuanzhongwenzimu.org.cn</code>

主题分类 C

<code>guochanjingpinzaixianmianfeikanb.org.cn</code>

<code>zhongwenzimuzaixianyingshiyuanb.org.cn</code>

## 项目结构

```text
novaindex/
├── app/                                # 核心应用模块
│   ├── controllers/                    # 路由控制器，处理 HTTP 请求与响应
│   ├── models/                         # 数据模型层，定义链接、分类、检测记录实体
│   ├── services/                       # 业务逻辑服务，包含索引管理、检测引擎、导入导出
│   └── views/                          # 模板渲染层，面向管理后台与静态站点生成
├── scripts/                            # 运维与开发辅助脚本
│   ├── init_config.py                  # 生成默认配置文件与初始分类模板
│   ├── migrate_db.py                   # 执行数据库结构迁移与版本升级
│   └── health_check.py                 # 手动触发完整链接可用性检测
├── tests/                              # 单元测试与集成测试用例
│   ├── unit/                           # 针对模型与服务层的细粒度测试
│   └── integration/                    # 端到端的 API 与检测流程测试
├── docs/                               # 完整文档源文件，采用 Markdown 编写
├── static/                             # 静态站点生成器的输出目录
│   ├── assets/                         # 构建后的 CSS、JavaScript 与图片资源
│   └── pages/                          # 生成的 HTML 页面按分类结构组织
├── config/                             # 运行环境配置目录
│   ├── development.yaml                # 开发环境参数，开启调试与热重载
│   └── production.yaml.example         # 生产环境配置模板，需自行复制并填写
├── logs/                               # 应用日志及检测报告存储位置
├── requirements.txt                    # Python 依赖清单，精确锁定版本
└── README.md                           # 项目入口说明文档
```

## 贡献指南

1. 查阅 issue 列表或通过讨论区发起新提案，确认当前需求未被他人认领，避免重复工作。
2. 派生项目仓库至个人账户，在派生副本中创建以 `feat/` 或 `fix/` 为前缀的功能分支。
3. 提交代码前运行 `make test` 执行完整测试套件，确保所有现有用例通过且新增逻辑有对应覆盖。
4. 更新 `docs/` 下受影响的相关文档，包括用户手册中涉及功能描述的部分以及 API 变更说明。
5. 发起合并请求至主仓库的 `develop` 分支，填写完整的变更描述与测试结果摘要，等待维护者审阅。

## 常见问题

**问：索引中的链接检测频率是多少？是否会占用大量网络带宽？**

答：系统默认每 24 小时对所有已标记为活跃的链接执行一次 HTTP HEAD 请求检测。检测任务采用协程并发控制，默认并发数为 20，超时时间设为 10 秒，单次完整检测（约 1000 条链接）的总流量消耗通常小于 5 MB。您可通过配置文件调整检测周期与并发参数。

**问：静态站点生成模式与动态管理后台的关系是什么？**

答：动态管理后台提供完整的索引编辑、检测触发与数据导入导出能力，适用于日常维护场景。静态站点生成模式则是将当前索引数据渲染为一组独立的 HTML 文件，该模式不依赖数据库或应用服务器，适合作为公开只读的知识库发布。两者共用同一套数据模型，生成操作可通过管理面板一键触发。

**问：如何从其他书签工具迁移数据至 Nova Index？**

答：项目内置了 CSV 导入适配器，支持大多数浏览器导出的书签文件格式（需预先转换为 UTF-8 编码的 CSV）。您只需保证每行包含标题与 URL 两列，系统将根据域名自动建议初始分类。对于更复杂的标签结构，建议先导出 JSON 模板，再按模板格式填充数据后重新导入。

## 许可证

MIT

> 外链数量: 7 | 生成时间: 2026-08-14 22:01:34
