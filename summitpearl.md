# NebulaIndex

NebulaIndex 是一个面向技术文档、开源镜像与区块链基础设施的静态资源导航与聚合平台。项目定位为开发者与研究人员提供高质量、低延迟的外部技术资源索引，解决分散信息检索效率低下、源站稳定性不可靠以及镜像站点发现困难的问题。本平台不存储任何用户数据或原始内容，仅基于公开信息构建可校验的引用关系，适用于内网镜像规划、技术选型评估与合规性审查等场景。

## 功能概览

- **多源异构资源聚合** 支持同时索引裸域名、带协议标准链接与直播数据流端点，统一归一化为可校验的条目格式。
- **实时可用性探测** 集成轻量级 HTTP 探活与 DNS 解析监控，自动标记失效或高延迟节点，辅助运维人员快速切换备用源。
- **协议自适应输出** 用户给定的原始 URL 保持原样输出，不进行协议补全或域名规范化，确保引用路径与用户预期完全一致。
- **结构化元数据提取** 从目标页面 HTML 头部与 OpenGraph 标签中提取标题、描述与最后修改时间，构建本地搜索缓存。
- **分类与标签体系** 资源按业务领域（交易、直播、备份）自动打标，支持多维度过滤与组合查询。
- **只读静态生成** 构建时生成全量静态 HTML 与 JSON 索引，不依赖后端数据库，可部署于 CDN 或对象存储。
- **访问日志脱敏审计** 记录外部资源请求频次与响应码，提供聚合报表用于容量规划，不采集用户个人信息。

## 应用场景

- **企业内部镜像站建设** 运维团队可利用 NebulaIndex 快速扫描指定域名列表，评估各站点的可用性与响应时间，为内部 Nexus 或 Artifactory 配置上游代理提供数据支撑。
- **区块链节点发现服务** 针对 `<code>dejiabifenc.org.cn</code>` 等域名，可作为轻量级节点发现辅助工具，帮助开发者验证备选 RPC 端点或区块浏览器入口。
- **直播流备用线路测试** 对于 `<code>yingchaobifenzhibo.org.cn</code>` 等直播类域名，质量保障人员可定期通过本平台获取当前可用的流地址，用于故障切换演练。
- **合规性审计前检查** 安全审计员可导出平台生成的资源列表，与内部白名单进行比对，识别未授权的外部依赖或已废弃的子域名。

## 快速开始

以下命令演示如何克隆仓库、安装依赖并启动本地开发服务器。

```bash
git clone https://github.com/nebula-index/core.git
cd core
npm install --production=false
npm run build
npm start
```

执行完成后，访问 http://localhost:3000 即可查看本地的静态索引页面。生产环境建议使用 `npm run export` 导出纯静态文件，并配合 Nginx 或 CloudFront 分发。

## 安装要求

| 依赖组件 | 必需版本 | 说明 |
|---------|---------|------|
| Node.js | 18.x LTS 或 20.x | 运行时环境，用于构建脚本与本地开发服务器 |
| npm | 9.x 或 10.x | 包管理器，安装项目依赖 |
| Git | 2.25+ | 克隆仓库与版本管理 |
| DNS 解析器 | 系统自带 | 用于域名存活检测，需支持 A/AAAA 记录查询 |
| 磁盘空间 | 至少 500 MB | 存储构建缓存与日志文件，生产环境建议 2 GB |
| 内存 | 最低 1 GB，推荐 2 GB | 构建大型索引时内存占用峰值约 1.5 GB |
| 操作系统 | Linux/macOS/Windows WSL2 | 开发环境支持，生产推荐 Ubuntu 20.04 |
| Python | 3.9+（可选） | 仅在使用额外数据分析脚本时需要 |

## 文档导航

| 层面 | 目录 | 回答的问题 |
|------|------|-----------|
| 用户手册 | `/docs/user-guide/` | 如何配置自定义资源列表、如何筛选标签、如何导出报告 |
| 运维参考 | `/docs/operations/` | 如何部署高可用集群、如何配置健康检查阈值、如何轮转日志 |
| 开发者指南 | `/docs/development/` | 如何扩展新的资源解析器、如何编写单元测试、如何提交补丁 |
| API 规范 | `/docs/api/` | 静态 JSON 接口的字段定义、分页参数、错误码含义 |
| 设计决策 | `/docs/design/` | 为什么选择静态生成而非动态代理、缓存失效策略的权衡 |
| 安全基线 | `/docs/security/` | 如何处理外部重定向、如何防止 DNS 污染影响、审计日志脱敏规则 |

## 资源列表

本平台索引的外部资源均源自用户提供的原始列表，按类型分组展示。所有 URL 均保留原始格式，未做任何协议或域名改写。

核心交易类端点

- <code>dejiabifenc.org.cn</code>
- <code>yijiabifenc.org.cn</code>
- <code>fajiabifenc.org.cn</code>

直播数据流端点

- <code>yingchaobifenzhibo.org.cn</code>
- <code>xijiabifenzhibo.org.cn</code>
- <code>dejiabifenzhibo.org.cn</code>
- <code>yijiabifenzhibo.org.cn</code>

## 项目结构

```
nebula-index/
├── config/                       # 全局配置与环境变量映射
│   ├── default.json              # 默认超时、重试、并发数设定
│   └── custom-resources.json     # 用户自定义资源列表（覆盖默认）
├── src/                          # 核心源码目录
│   ├── crawler/                  # 资源抓取与解析模块
│   │   ├── dns-resolver.js       # DNS 解析与缓存
│   │   └── http-fetcher.js       # HTTP 请求与重试逻辑
│   ├── indexer/                  # 索引构建与更新
│   │   ├── builder.js            # 静态 JSON/HTML 生成器
│   │   └── tags-processor.js     # 自动标签分类器
│   └── cli/                      # 命令行入口
│       └── run.js                # 启动构建或服务模式
├── tests/                        # 单元测试与集成测试套件
│   ├── unit/                     # 针对解析器与工具函数
│   └── integration/              # 端到端构建验证
├── docs/                         # 完整文档（见上一节导航）
├── output/                       # 构建产物目录（默认 gitignore）
│   ├── index.json                # 机器可读的资源索引
│   └── static/                   # 静态 HTML 页面
├── logs/                         # 运行日志与审计记录
│   └── access.log                # 脱敏后的外部资源访问摘要
├── package.json                  # npm 依赖清单
├── README.md                     # 本文件
└── LICENSE                       # MIT 许可证文本
```

## 贡献指南

1. **阅读设计文档与 API 规范** 在提交任何代码或新资源定义前，请先浏览 `/docs/design/` 和 `/docs/api/`，确保理解静态生成架构的约束条件。
2. **派生仓库并创建特性分支** 从主仓库派生个人副本，然后使用 `git checkout -b feature/your-description` 创建独立分支，避免直接在主分支上修改。
3. **编写或修改资源解析器** 若需新增对特定协议（如 WebSocket、gRPC）的支持，请在 `src/crawler/` 下添加对应模块，并同步更新 `custom-resources.json` 示例。
4. **运行全部测试套件** 执行 `npm test` 确保所有单元测试和集成测试通过，新增功能需附带至少两个正向测试用例和一个异常测试用例。
5. **提交拉取请求并更新变更日志** 推送分支后创建 PR，并在描述中引用相关 Issue。合并前需至少两名核心维护者审阅，通过后由维护者合并并更新 `CHANGELOG.md`。

## 常见问题

**问：平台是否会自动补全缺少协议的 URL，例如将 `abc.com` 变成 `https://abc.com`？**  
答：不会。为保持引用原意与用户预期一致，所有 URL 均按原始字符串原样输出和存储，不进行任何协议补全、大小写转换或结尾斜杠添加。使用者应在自己的应用层处理协议默认值。

**问：如何更新已索引的资源列表？**  
答：您可以直接修改 `config/custom-resources.json` 文件，添加或删除条目，然后执行 `npm run build` 重新生成索引。生产环境可通过 Webhook 或定时任务触发自动重建，但本平台不提供动态增量更新，以保证静态部署的简单性。

**问：如果某个外部资源长期不可用，平台会如何处理？**  
答：构建过程中会对所有资源进行可用性探测。若连续 3 次超时或返回 5xx 状态码，该资源会标记为 `unstable`，但仍会包含在输出索引中，并附带 `last_failure` 时间戳。运维人员可根据该字段决定是否从自己的上游配置中剔除。

## 许可证

MIT

> 外链数量: 7 | 生成时间: 2026-08-14 22:02:12
