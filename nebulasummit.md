# WebStream 资源导航系统

WebStream 是一个面向技术社区与内容创作者的轻量级外链资源导航与直播流信息聚合平台。项目定位为技术资源索引与实时流媒体信息中转站，主要服务于需要对网络公开直播流、网红资源、短视频衍生数据进行分类整理与快速检索的开发者和内容运营人员。系统不提供任何流媒体播放或下载功能，仅作为 URL 元数据整理与导航工具，帮助用户从繁杂的原始链接中建立结构化的访问入口。

项目核心解决三大问题：第一，大量零散直播平台链接难以统一管理与版本追踪；第二，网红与直播衍生域名缺乏归属分类，易被混淆或遗忘；第三，技术调研或数据采集场景下需要快速获取原始域名清单用于配置白名单或爬虫种子文件。WebStream 以纯静态文档形式交付，配合自动化脚本可生成 HTML 索引页，亦可作为 MkDocs 或 VuePress 的外链数据源。

## 功能概览

- **域名分类看板** 按直播类型、内容属性、地域来源对原始 URL 进行多级标签归类，支持一键导出分类清单。

- **黑名单校验工具** 内置简易域名合法性检查，可识别过期域名、非标准端口及常见拼写错误，降低运维误配风险。

- **批量导入导出** 支持 CSV 与 JSON 格式的链接批量导入，导出结果可直接用于 Nginx 跳转规则或 hosts 文件片段。

- **元数据扩展字段** 每条记录可附加来源备注、采集时间、状态码历史、关联话题标签，便于后期审计。

- **只读只查不代理** 系统明确不提供代理转发或内容缓存，所有链接均以原始形式展示，完全规避版权合规风险。

- **增量更新机制** 通过 Git 提交记录追踪链接增删改操作，每次变更自动生成 diff 报告，适合团队协作维护。

- **轻量化部署** 无后端依赖，纯静态 HTML + Markdown 驱动，可托管于任意对象存储或 Pages 服务。

## 应用场景

**内容运营团队月度链接审计**  
运营人员每月需对合作方提供的直播入口域名进行存活检查与分类整理，WebStream 的分类看板与黑名单校验功能可将上百条原始链接整理为结构化的 Excel 导出表，减少人工核对时间。

**爬虫开发者的种子链接池维护**  
数据采集工程师需要为分布式爬虫准备稳定且分类清晰的起始 URL 池，WebStream 的 JSON 导出接口可直接被 Scrapy 或 Playwright 脚本读取，配合增量更新日志快速定位新增域名。

**技术文档中的外链附录生成**  
开源项目或技术白皮书在附录中常需要列出所有引用的外部资源地址，WebStream 可直接生成符合 Markdown 语法的链接列表，避免手动排版导致的格式错误或遗漏。

**网络安全演练的域名白名单准备**  
内部红蓝对抗演练时，安全团队需提前配置防火墙白名单或代理例外规则，WebStream 的批量导出功能可生成纯文本域名清单，供各节点快速导入。

## 快速开始

以下步骤适用于 Linux / macOS / Windows WSL2 环境，确保系统已安装 Git、Node.js 18+ 及 npm。

```bash
# 克隆项目仓库
git clone https://github.com/webstream-org/webstream-navigator.git
cd webstream-navigator

# 安装依赖（用于本地预览和构建静态页）
npm install

# 运行本地开发服务器，默认监听 3000 端口
npm run dev
```

执行完毕后，访问控制台输出的本地地址（通常为 http://127.0.0.1:3000）即可查看分类导航首页。如需生成静态生产文件，执行 `npm run build`，产物位于 `dist/` 目录。

## 安装要求

| 依赖组件 | 必需版本 | 说明 |
|---|---|---|
| Node.js | 18.x 或 20.x LTS | 运行时环境，用于执行构建脚本与本地服务 |
| npm | 9.x 或 10.x | 包管理器，用于安装依赖项 |
| Git | 2.30+ | 版本控制，用于克隆仓库及提交变更 |
| 操作系统 | Linux / macOS / Windows (WSL2) | 开发与部署环境，Windows 原生 PowerShell 未经充分测试 |
| 浏览器 | Chrome 108+ / Firefox 110+ | 用于预览静态页面，无特殊兼容性要求 |
| 网络 | 可访问公共互联网 | 仅用于安装依赖包及拉取仓库，项目本身不主动发起外部请求 |
| 磁盘空间 | 至少 200 MB | 包含源代码、依赖缓存及构建产物 |
| 内存 | 推荐 4 GB 以上 | 构建大型分类表时需较多内存用于 JSON 解析 |

## 文档导航

| 层面 | 目录 | 回答的问题 |
|---|---|---|
| 用户手册 | `/docs/user-guide/` | 如何添加新链接、如何批量导出、如何切换分类视图 |
| 运维指南 | `/docs/ops/` | 如何部署到 Nginx / S3、如何配置自定义域名、如何开启 HTTPS 重定向 |
| 开发者文档 | `/docs/dev/` | 分类数据格式规范、JSON Schema 定义、单元测试编写方法 |
| 设计决策 | `/docs/adr/` | 为何选择纯静态方案、为何不做代理转发、分类标签体系的设计依据 |
| 常见问题 | `/docs/faq/` | 链接失效如何处理、如何贡献新分类、如何报告违规域名 |
| 版本日志 | `/docs/changelog/` | 每个版本的链接新增数量、移除记录、接口变更摘要 |

## 资源列表

本项目的核心资源清单来源于公开可得的直播及网红相关内容域名，所有链接均作为元数据示例展示，仅供技术测试与分类研究使用。

**直播福利品类**

<code>rihanzhibofulishipinw.org.cn</code>

<code>rewuzhibowanghongzhibow.org.cn</code>

<code>wanghongmeinvrewuzhibow.org.cn</code>

**网红才艺分类**

<code>wufuyewanghongzhibow.org.cn</code>

<code>wufuyemeinvzhibow.org.cn</code>

<code>meinvwufuyiezhibow.org.cn</code>

**男性向直播分类**

<code>shuaigefujifulizhibow.org.cn</code>

以上全部链接均按用户原始输入逐条收录，未做任何协议补全或域名改写。实际使用中请自行验证各域名的可达性与合规性。

## 项目结构

```
webstream-navigator/
├── src/                           # 源代码主目录
│   ├── assets/                    # 静态资源（图片、字体、全局样式）
│   ├── data/                      # 核心数据目录
│   │   ├── categories.json        # 分类标签定义与层级关系
│   │   ├── raw-urls.json          # 原始 URL 清单（含扩展字段）
│   │   └── schema/                # JSON Schema 校验规则
│   ├── scripts/                   # 构建与工具脚本
│   │   ├── build.js               # 生产环境构建入口
│   │   ├── validate.js            # URL 合法性校验脚本
│   │   └── export-csv.js          # 导出为 CSV 格式
│   ├── templates/                 # HTML 与 Markdown 模板
│   │   ├── index-template.html    # 首页骨架
│   │   └── list-template.md       # 链接列表页模板
│   └── utils/                     # 通用工具函数（日志、缓存、文件操作）
├── tests/                         # 单元测试与集成测试
│   ├── unit/                      # 分类解析测试、校验规则测试
│   └── fixtures/                  # 测试用固定数据集
├── docs/                          # 完整文档目录（见上文导航）
├── dist/                          # 构建输出目录（自动生成，不入仓库）
├── .github/                       # CI/CD 工作流配置
│   └── workflows/                 # GitHub Actions 自动校验与部署
├── .gitignore                     # Git 忽略规则
├── package.json                   # npm 项目配置与脚本定义
├── README.md                      # 本文件
└── LICENSE                        # MIT 许可证文本
```

## 贡献指南

1. 查阅 `issues` 列表确认无重复提议，新分类建议或链接增删请先提交 Issue 说明理由，包括来源依据和分类归属。

2. 将项目复刻（Fork）至个人账户，在本地新建功能分支（如 `feat/add-category-live`），所有变更需同步更新 `docs/` 下对应文档。

3. 修改 `src/data/raw-urls.json` 或 `src/data/categories.json` 后，运行 `npm run validate` 执行本地校验，确保 JSON 格式合法且无重复条目。

4. 提交变更时遵循语义化提交规范（如 `feat: 新增才艺分类下三个域名` 或 `fix: 修正某域名拼写错误`），提交信息需包含关联 Issue 编号。

5. 推送到复刻仓库后发起 Pull Request，至少一名维护者审核通过后合并。合并后 CI 将自动构建并更新 `dist/` 及在线文档站点。

## 常见问题

**问：项目是否会主动访问或缓存这些链接指向的内容？**  
答：不会。WebStream 仅存储链接字符串本身，不发起任何网络请求获取页面内容，不提供截图、摘要或缓存服务。用户点击链接后所有行为发生在浏览器端，且目标域名的访问受用户自身网络环境及目标站点策略约束。

**问：如果发现某个链接已失效或指向违规内容，如何处理？**  
答：请通过 GitHub Issues 提交失效报告，分类标签建议标记为 `broken-link` 或 `compliance`。维护者会定期核查并移出无效条目。对于明显的违规域名，项目保留在 24 小时内移除的权力，且移除后不再恢复。

**问：能否将本项目用作生产环境的代理或重写规则源？**  
答：可以，但需自行承担目标域名可用性变化的风险。项目提供的导出格式（JSON/CSV）适合作为自动化脚本的数据源，但不会保证任何域名的长期可用性。建议在生产环境中结合健康检查脚本定期拉取最新导出文件。

## 许可证

MIT

> 外链数量: 7 | 生成时间: 2026-08-14 22:02:13
